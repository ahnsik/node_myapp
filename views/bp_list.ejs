<!DOCTYPE html>
<html lang="kr" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="user-scalable=no, width=device-width" />
    <link rel="stylesheet" type="text/css" href="carstyle.css" />
    <title>혈압 기록 리스트</title>
    <style type="text/css">
      * {
        margin: 0;
        font-size: 12px;
      }
      table {
        border-collapse: collapse;
      }
    </style>
  </head>
  <body>
      <H3>최근 입력된 체성분 기록을 보여줌.</H3>

      <table border='1px'>
          <!-- <colgroup>
              <col width='60'><col width='80'><col width='200'><col width='100'>
          </colgroup> -->
          <thead>
              <tr>
                  <th>날짜</th>
                  <th>혈압(수축기/이완기)</th>
                  <th>맥박</th>
                  <th>체중</th>
                  <th>체지방률</th>
                  <th>비만지수</th>
                  <th>기초대사량</th>
                  <th>BMI</th>
              </tr>
          </thead>
          <tbody>
              <% for(i = 0; i < list.length; i++) { %>
               <tr>
                <td><%=list[i].date_written %></td>

                var params = [body.bp_high, body.bp_low, body.h_beat, body.weight, body
                ., body.fatness, body.metabolism, body.bmi, body.slect_time, body.stretching, body.aerobic_workout, body.muscle_workout, body.diary_subject, "" ];

                <td><%=list[i].bp_high %> / <%=list[i].bp_low %> hmg</td>
                <td><%=list[i].h_beat %>회</td>
                <td><%=list[i].weight %> kg</td>
                <td><%=list[i].body_fatrate %> %</td>
                <td><%=list[i].metabolism %> </td>
                <td><%=list[i].bmi %> kg</td>
              </tr>
              <% } %>
          </tbody>
      </table boader='1'>

      <!-- <script type="text/javascript"> -->
      <%
        function  DateFormat(d) {
          return  d.getFullYear()+"-"+(d.getMonth()+1)+"-"+d.getDate();
        } %>
      <!-- </script> -->
  </body>
</html>
